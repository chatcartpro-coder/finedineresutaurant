# credentials/

This folder is for any local secret files you don't want committed to git
(e.g. a future POS API key file). Currently unused - all secrets for this
project are supplied via environment variables (.env locally, host env vars
in production). See `.env.example` for the full list.
